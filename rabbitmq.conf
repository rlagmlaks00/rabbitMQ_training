# guest 계정은 기본적으로 localhost(루프백)에서의 연결만 허용됨.
# Docker 포트포워딩을 통해 호스트에서 접속하면 브로커 입장에서는 non-loopback 연결로 보여
# guest/guest 로는 ACCESS_REFUSED가 발생함 -> 테스트 편의를 위해 제약 해제.
# (운영 환경에서는 guest 대신 별도 계정을 사용하고 이 설정을 제거할 것)
loopback_users = none
